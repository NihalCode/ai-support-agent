# {{APP_TITLE}}

Generated by AI Support Agent Build App mode.

## Setup

1. Copy `.env.local.example` → `.env.local` and fill in credentials.
2. Run `npm install && npm run dev`.
3. Open http://localhost:3000

Server-side API routes keep Cyware credentials off the frontend.
